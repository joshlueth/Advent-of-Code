#pragma once
#include <vector>

bool recursion(long long,std::vector<long long>);
bool bruteForce(long long,long long,std::vector<long long>);
int checkMultiply(long long&,long long);
void subtract(long long&,long long);
long long concatenate(long long, long long);
